[project]
name = "curtail-core"
version = "0.1.0"
description = "Deterministic allocation core. No LLM sits in the allocation path."
requires-python = ">=3.13,<3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/curtail_core"]
