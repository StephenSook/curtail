# What the DEPLOYED service serves

Generated by `scripts/probe_deployment.py`. Do not hand-edit: a hand-written
line inside a generated file is the one line nothing checks.

This file exists because `docs/FACTS.md` computes its claims from repository
source, and repository source cannot see that production is stale. Nothing here
deploys on merge, so the repository can be correct and the live URL behind it.

- Service: https://curtail-console-api-672785135387.us-central1.run.app
- Probed at repository commit: `5099c391e9338c12dce6519db91318ce258f56a0`

## Routes the live service advertises

- `/`
- `/api/basins`
- `/api/classify/{basin}`
- `/api/facts`
- `/api/fleet/{basin}`
- `/api/healthz`
- `/api/queue`
- `/api/queue/draft/{basin}`
- `/api/queue/{order_id}`
- `/api/queue/{order_id}/sign`
- `/api/recommendation/{basin}`
- `/api/session`

12 routes.

## Capabilities the repository may claim

| Capability | Route that settles it | Served |
|---|---|---|
| an officer signs the order | `/api/queue/{order_id}/sign` | yes |
| the console runs the graph | `/api/fleet/{basin}` | yes |
| the service serves its own fact sheet | `/api/facts` | yes |

Every capability the repository claims is reachable on the live service.
